# KRS Studio

A tiny web app to control a camera that is connected to WiFi. It works by sending `POST` requests to the camera's IP, which contain the view ID we want to switch to.

Built with Svelte.
